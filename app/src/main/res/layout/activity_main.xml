<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    tools:context=".MainActivity">

    <!-- Hero Section with Background Image -->
    <LinearLayout
        android:id="@+id/heroSection"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/hero_background"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent="0.5">

        <!-- Header Title with Gradient Overlay -->
        <TextView
            android:id="@+id/headerTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_rounded_light"
            android:padding="16dp"
            android:text="Scan something fun!"
            android:textColor="#333333"
            android:textSize="24sp"
            android:textStyle="bold"
            android:shadowColor="#80000000"
            android:shadowDx="2"
            android:shadowDy="2"
            android:shadowRadius="4"
            android:layout_marginBottom="24dp" />

        <!-- Camera Hint -->
        <TextView
            android:id="@+id/tvCameraHint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:shadowColor="#80000000"
            android:shadowDx="2"
            android:shadowDy="2"
            android:shadowRadius="4"
            android:text="🌟 Discover the world around you! 🌟"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:textStyle="bold" />

        <!-- Button Container for better layout -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginBottom="16dp">

            <!-- Camera Button -->
            <Button
                android:id="@+id/button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_rounded_yellow"
                android:text="📸 Camera"
                android:textColor="#333333"
                android:textSize="16sp"
                android:textStyle="bold"
                android:drawableLeft="@drawable/ic_camera_kid"
                android:drawablePadding="8dp"
                android:padding="20dp"
                android:elevation="8dp"
                android:layout_marginEnd="12dp" />

            <!-- Gallery Button -->
            <Button
                android:id="@+id/learningButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🖼️ Gallery"
                android:textColor="#333333"
                android:textSize="16sp"
                android:textStyle="bold"
                android:background="@drawable/bg_rounded_light"
                android:drawableLeft="@drawable/ic_gallery_kid"
                android:drawablePadding="8dp"
                android:padding="20dp"
                android:elevation="6dp" />

        </LinearLayout>

    </LinearLayout>

    <!-- Ocean Wavy Separator -->
    <ImageView
        android:id="@+id/waveSeparator"
        android:layout_width="421dp"
        android:layout_height="51dp"
        android:scaleType="fitXY"
        android:src="@drawable/wave_separator"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/heroSection" />

    <!-- Content Section -->
    <ScrollView
        android:id="@+id/contentScroll"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@id/waveSeparator"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center">

            <!-- Fun Activities Title -->

            <!-- Quick Actions Row -->
            <TextView
                android:id="@+id/tvQuickActions"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:layout_marginBottom="20dp"
                android:text="🎮 Fun Activities 🎮"
                android:textColor="#333333"
                android:textSize="22sp"
                android:textStyle="bold" />

            <LinearLayout
                android:id="@+id/quickActionsRow"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center"
                android:layout_marginBottom="24dp">

                <Button
                    android:layout_width="0dp"
                    android:layout_height="64dp"
                    android:layout_weight="1"
                    android:layout_marginEnd="6dp"
                    android:text="📷\nCamera"
                    android:textColor="#333333"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:background="@drawable/bg_rounded_light"
                    android:elevation="3dp"
                    android:gravity="center"/>

                <Button
                    android:layout_width="0dp"
                    android:layout_height="64dp"
                    android:layout_weight="1"
                    android:layout_marginStart="3dp"
                    android:layout_marginEnd="3dp"
                    android:text="⭐\nFavorites"
                    android:textColor="#333333"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:background="@drawable/bg_rounded_light"
                    android:elevation="3dp"
                    android:gravity="center"/>

                <Button
                    android:id="@+id/btnQuickRecent"
                    android:layout_width="0dp"
                    android:layout_height="64dp"
                    android:layout_weight="1"
                    android:layout_marginStart="6dp"
                    android:text="🕒\nRecent"
                    android:textColor="#333333"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:background="@drawable/bg_rounded_light"
                    android:elevation="3dp"
                    android:gravity="center"/>
            </LinearLayout>

            <!-- History Button -->
            <Button
                android:id="@+id/btnHistory"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🎓 See all learnt objects! 🎓"
                android:textColor="#333333"
                android:textSize="16sp"
                android:textStyle="bold"
                android:background="@drawable/bg_rounded_yellow"
                android:padding="20dp"
                android:elevation="6dp"
                android:layout_marginBottom="24dp"/>

            <!-- Captured Image -->
            <ImageView
                android:id="@+id/imageView"
                android:layout_width="240dp"
                android:layout_height="240dp"
                android:layout_marginTop="16dp"
                android:background="@drawable/bg_rounded_card"
                android:scaleType="centerCrop"
                android:visibility="gone"
                android:elevation="6dp"/>

            <!-- Result Text -->
            <TextView
                android:id="@+id/resultTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:background="@drawable/bg_rounded_card"
                android:padding="16dp"
                android:textSize="14sp"
                android:textColor="#333333"
                android:visibility="gone"
                android:elevation="4dp"/>

            <!-- Recent Scans Section (hidden until opened) -->
            <TextView
                android:id="@+id/tvRecentsTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🌟 Your Recent Discoveries! 🌟"
                android:textStyle="bold"
                android:textSize="18sp"
                android:textColor="#333333"
                android:visibility="gone"
                android:layout_marginTop="24dp"
                android:layout_marginBottom="12dp"/>

            <ScrollView
                android:id="@+id/recentsScroll"
                android:layout_width="match_parent"
                android:layout_height="200dp"
                android:visibility="gone"
                android:fillViewport="true"
                android:background="@drawable/bg_rounded_light"
                android:elevation="4dp">

                <LinearLayout
                    android:id="@+id/recentList"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="12dp"/>
            </ScrollView>

        </LinearLayout>
    </ScrollView>
</androidx.constraintlayout.widget.ConstraintLayout>
